# Example environment file for OpenClaw provider configuration
# Copy to ".env" and fill in the real API key (keep the real file out of version control).

# Modal API key (DO NOT commit this file with real keys)
MODAL_API_KEY=replace_with_your_real_modal_api_key_here

# Optional: other env vars used by OpenClaw can go here
